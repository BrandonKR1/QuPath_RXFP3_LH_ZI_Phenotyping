//Batch apply object classifiers. This script should be used to batch apply pre-trained object classifiers to all (or a subset) of images in the project. 
// Just replace the classifier names below with your pretrained classifiers. Press the three dots on the bottom right of the window and click 'Run for project'.
// Then choose which images to apply the script to. Then you're done :)

selectAnnotations();
runObjectClassifier("classifier1", "classifier2", "classifier3");
